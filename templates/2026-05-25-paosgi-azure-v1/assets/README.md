# assets/ — intentionally minimal

This template uses **inline SVG only** (no external image assets needed).
All visual elements are defined inside `index.html`'s `<defs>` block as
ORIGINAL self-drawn geometric primitives.

Business team can drop their own logos / portraits / banners here if they
want to switch from inline SVG to image-based later.
